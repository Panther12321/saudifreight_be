SITE_NAME=naqil.localhost
SITE_DB_NAME=naqil_frappe
SITE_DB_PASSWORD=replace-with-a-local-site-database-secret
DB_ROOT_PASSWORD=replace-with-a-local-development-secret
ADMIN_PASSWORD=replace-with-a-temporary-administrator-password

# Set these only in Railway variables or a local .env file. Never commit real values.
FRAPPE_ENCRYPTION_KEY=
